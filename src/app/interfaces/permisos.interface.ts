export interface Permisos {
    gestiona_inventario?: boolean;
    gestiona_clientes?: boolean;
    gestiona_ventas?: boolean;
    gestiona_soporte?: boolean;
    gestiona_analitica?: boolean;
    gestiona_permisos?: boolean;
}
